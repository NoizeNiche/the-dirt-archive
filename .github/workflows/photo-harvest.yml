name: Harvest pedal photos

on:
  push:
    branches: [main]
    paths-ignore:
      - 'public/catalog-photo-manifest.js'
      - 'research/catalog-photo-harvest-status-01.tsv'
  schedule:
    - cron: '17 6 * * 0'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  harvest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Harvest photo references
        run: python scripts/photo_harvest.py
      - name: Commit manifest
        uses: stefanzweifel/git-auto-commit-action@v7
        with:
          commit_message: 'chore: refresh full-catalog pedal photo manifest'
          file_pattern: 'public/catalog-photo-manifest.js research/catalog-photo-harvest-status-01.tsv'
