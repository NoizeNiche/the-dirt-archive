Photo recovery kick v5
Issued September 22, 2026. Exercise AMT leads and expanded lazy-image extraction.
Purpose: start one bounded recovery pass against the current main branch after scheduled delivery stopped producing cache runs.
This file is intentionally inert data. Only changes to this exact sentinel path trigger the cache workflow.
