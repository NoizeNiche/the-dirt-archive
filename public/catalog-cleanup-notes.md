# Catalog Cleanup

The catalog presentation is being consolidated before Protocol A resumes.

The numbered catalog extension files are treated as historical implementation layers. New presentation work should prefer the canonical application and data model instead of adding another numbered extension.

The cleanup keeps research/evidence while simplifying the public interface.
