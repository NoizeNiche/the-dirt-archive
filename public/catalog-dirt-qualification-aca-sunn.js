(()=>{const nativeFetch=window.fetch.bind(window);let merged=false;const qualified=[{builder_id:'BLD-QUAL-ACA-01',name:'Amplifier Corporation of America (ACA)',country:'USA',status:'Verified dirt maker',founded:1967,dirt_count:1,description:'American manufacturer documented for the Fuzz King / Fuzz Sound of 1967-68.',dirt_evidence:'Fuzz King / Fuzz Sound',qualification:'verified_dirt_maker'},{builder_id:'BLD-QUAL-SUNN-01',name:'Sunn',country:'USA',status:'Verified dirt maker',founded:1966,dirt_count:1,description:'American amplifier maker documented for the Sunn Buzz SB-1G fuzz/distortion.',dirt_evidence:'Sunn Buzz SB-1G',qualification:'verified_dirt_maker'}];window.fetch=async(input,init)=>{const response=await nativeFetch(input,init);const url=new URL(typeof input==='string'?input:input.url,location.href);if(!url.pathname.endsWith('/data.json')||merged)return response;try{const base=await response.clone().json();base.builders=Array.isArray(base.builders)?base.builders:[];for(const q of qualified){const existing=base.builders.find(b=>String(b.name||'').trim().toLowerCase()===q.name.toLowerCase()||b.builder_id===q.builder_id);if(existing){existing.qualification=q.qualification;existing.status=q.status;existing.dirt_evidence=q.dirt_evidence;existing.dirt_count=Math.max(Number(existing.dirt_count||0),q.dirt_count)}else base.builders.push({...q})}merged=true;return new Response(JSON.stringify(base),{status:200,headers:{'Content-Type':'application/json'}})}catch(err){console.error('ACA/Sunn qualification overlay failed:',err);return response}}})()