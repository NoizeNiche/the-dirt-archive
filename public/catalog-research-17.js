(() => {
  const nativeFetch=window.fetch.bind(window); let merged=false;
  const records={
    'Karma Suture GE':{status:'Research',summary:'Catalinbread Karma Suture germanium family record.',identification_notes:'GE is an electronics/product identity and should not be reduced to an enclosure-color distinction. Preserve exact model labeling and specimen provenance.',lineage_notes:'Karma Suture family.',variant_notes:['Germanium variant'],uncertainty:'Complete production chronology and all enclosure variants remain under research.',sources:[{title:'Catalinbread Manuals',url:'https://catalinbread.com/pages/manuals',type:'manufacturer',confidence:'High'}]},
    'Karma Suture SI':{status:'Research',summary:'Catalinbread Karma Suture silicon family record.',identification_notes:'SI is an electronics/product identity and should remain separate from the GE variant even if the enclosure is visually similar.',lineage_notes:'Karma Suture family.',variant_notes:['Silicon variant'],uncertainty:'Complete production chronology and all enclosure variants remain under research.',sources:[{title:'Catalinbread Manuals',url:'https://catalinbread.com/pages/manuals',type:'manufacturer',confidence:'High'}]}
  };
  async function merge(base){const b=base.builders?.find(x=>x.builder_id==='BLD-CAT-0001');if(!b)return base;(base.pedals||[]).forEach(p=>{if(p.primary_builder_id!==b.builder_id)return;const r=records[p.model_name];if(r)p.archive_research={...(p.archive_research||{}),...r};});return base;}
  window.fetch=async(input,init)=>{const response=await nativeFetch(input,init);const url=new URL(typeof input==='string'?input:input.url,location.href);if(!url.pathname.endsWith('/data.json')||merged)return response;try{const base=await response.clone().json();const data=await merge(base);merged=true;return new Response(JSON.stringify(data),{status:200,headers:{'Content-Type':'application/json'}});}catch(e){console.error('Catalinbread research 17 failed:',e);return response;}};
})();
