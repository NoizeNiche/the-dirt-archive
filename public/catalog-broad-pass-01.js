(() => {
  // Broad photo-census marker. Individual cleared images live in
  // catalog-cleared-images.js so this layer remains data-only and easy to extend.
  window.DIRT_BROAD_PASS_READY = true;
})();
